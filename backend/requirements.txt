# ── FastAPI (sin cambios) ─────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# ── InfluxDB (reemplaza SQLAlchemy + psycopg2) ────────────────────────────────
influxdb-client==1.43.0

# ── MQTT bridge ───────────────────────────────────────────────────────────────
paho-mqtt==2.1.0

# ── ELIMINADOS (entrega anterior) ────────────────────────────────────────────
# sqlalchemy==2.0.30       → reemplazado por influxdb-client
# psycopg2-binary==2.9.9   → no hay Postgres en esta entrega
